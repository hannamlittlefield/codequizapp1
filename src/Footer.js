import { AppBar } from '@material-ui/core'
import React from 'react'


export default function Footer(){
    return (
        <AppBar className="Footer" position="sticky">'This is the footer la la la'</AppBar>
    );
}